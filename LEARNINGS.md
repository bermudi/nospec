# Learnings

Durable insights that don't fit in an ADR (not a ruling) or AGENTS.md (not operational). Append-only. Each entry: date, what was learned, where it bites.

`AGENTS.md` stays the place for operational gotchas — build commands, test conventions, loop behavior. `LEARNINGS.md` is for domain or problem insights: "X doesn't work because Y", "the parser has this surprising property", "this assumption failed in production".

## 2026-07-10 — Placeholder

This file is intentionally empty until the project accumulates a real domain or problem insight. Seed the first entry when the codebase teaches you something that doesn't belong in an ADR or AGENTS.md.
